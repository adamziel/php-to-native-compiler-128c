# Loop Memory

- Repository starts from scratch in `adamziel/php-to-native-compiler-128c`.
- Do not import assumptions from older compiler repos unless explicitly verified.
- Progress must be denominator-based and primary-integrated.

