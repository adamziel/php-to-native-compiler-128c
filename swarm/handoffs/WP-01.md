# WP-01 Handoff

## Summary

Milestone: M4 parser / M6 WordPress blocker reduction.

Ported the useful part of WP-01 onto current `main` as a general PHP parser slice: the parser now skips whitespace plus PHP block/docblock comments and `//`/`#` line comments before supported statements. This reduces the current WordPress bootstrap blocker without WordPress-specific compiler behavior.

The pinned WordPress bootstrap check now advances past the opening `wp-settings.php` docblock and reports the next general parser gap at `define( 'WPINC', 'wp-includes' )`.

## Files Changed

- `crates/phpc_core/src/parser.rs`
- `docs/SUPPORT.md`
- `docs/WORDPRESS_COMPATIBILITY.md`
- `swarm/blockers.md`
- `swarm/handoffs/WP-01.md`
- `swarm/integration.md`
- `swarm/test-matrix.md`
- `swarm/wordpress-manifest.json`

## Tests Run

- `CARGO_TARGET_DIR=/home/ubuntu/phpc-targets/main-comment-trivia CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test -p phpc_core parser::tests -- --nocapture`
- `CARGO_TARGET_DIR=/home/ubuntu/phpc-targets/main-comment-trivia CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 cargo run -p phpc -- wordpress-bootstrap-check /home/ubuntu/phpc-external/wordpress/wordpress`
- `CARGO_TARGET_DIR=/home/ubuntu/phpc-targets/main-comment-trivia CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 cargo test`
- `CARGO_TARGET_DIR=/home/ubuntu/phpc-targets/main-comment-trivia CARGO_BUILD_JOBS=1 CARGO_INCREMENTAL=0 RUST_TEST_THREADS=1 scripts/local-gate.sh`
- `git diff --check`

## Pass/Fail State

- Pass: focused parser tests, 10 passed.
- Pass: pinned WordPress bootstrap check reports all five entrypoints present and advances to `define( 'WPINC', 'wp-includes' )`.
- Pass: full workspace `cargo test`, 61 passed.
- Pass: local coordination gate.
- Pass: diff whitespace check.

## Blockers

- WordPress bootstrap remains blocked on general PHP constant-definition/function-call parsing and execution.

## Latest Commit

- Current-main port: `9204262 Skip PHP comments before statements`.

## Next Suggested Slice

- Minimize `define( 'NAME', literal );` into a general PHP parser/interpreter fixture and either implement constant-definition behavior or emit a sharper unsupported diagnostic. Keep it independent of WordPress paths.
